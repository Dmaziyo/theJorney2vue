module.exports = {
  prefix: 'sd',
  // 用于给for指令的标签标记块
  BLOCK: 'sd-block',
  EACH: 'sd-each',
  CONTROLLER: 'sd-controller',
  mutatorMethods: ['pop', 'push', 'reverse', 'shift', 'unshift', 'splice', 'sort']
}
